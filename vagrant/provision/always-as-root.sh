#!/usr/bin/env bash

source /app/vagrant/provision/common.sh

#== Provision script ==

# sudo service nginx restart
# /etc/selenoid/./run.sh
# TODO: Restart the services whenever the VM is started, needed to refresh the config (first time almost)

<nav id="main-navbar" class="fixed top-0 left-0 right-0 z-50 bg-white/80 backdrop-blur-xl border-b border-sukarix-primary/10 transition-all duration-300">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex items-center justify-between h-16 lg:h-20">
            <a href="{{ @ALIASES.home }}" class="flex items-center space-x-3 group">
                <img src="images/logo.png" alt="Sukarix" class="h-8 w-auto">
            </a>

            <div class="hidden md:flex items-center space-x-8">
                <a href="{{ @ALIASES.home }}" class="nav-link text-sukarix-ink/75 hover:text-sukarix-magenta font-medium transition-colors" data-i18n-html="lbl.nav.home">{{ @i18n.label.nav.home }}</a>
                <a href="#features" class="nav-link text-sukarix-ink/75 hover:text-sukarix-magenta font-medium transition-colors" data-i18n-html="lbl.nav.features">{{ @i18n.label.nav.features }}</a>
                <a href="https://docs.sukarix.com" target="_blank" rel="noopener" class="nav-link text-sukarix-ink/75 hover:text-sukarix-magenta font-medium transition-colors" data-i18n-html="lbl.nav.documentation">{{ @i18n.label.nav.documentation }}</a>

                <div class="relative group">
                    <button type="button" class="lang-switcher-btn flex items-center space-x-2 text-sukarix-ink/75 hover:text-sukarix-magenta font-medium transition-colors focus:outline-none" aria-expanded="false">
                        <span class="text-lg leading-none">
                            <repeat group="{{ @i18n.list.locales }}" value="{{ @localeName }}" key="{{ @localeCode }}">
                                <check if="@localeCode == @current_locale">{{ @locale_flags[@localeCode] }}</check>
                            </repeat>
                        </span>
                        <span class="current-language-label">
                            <repeat group="{{ @i18n.list.locales }}" value="{{ @localeName }}" key="{{ @localeCode }}">
                                <check if="@localeCode == @current_locale">{{ @localeName }}</check>
                            </repeat>
                        </span>
                        <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                            <path stroke-linecap="round" stroke-linejoin="round" d="M19 9l-7 7-7-7"/>
                        </svg>
                    </button>
                    <div class="absolute right-0 mt-3 w-52 rounded-2xl shadow-2xl bg-white ring-1 ring-sukarix-primary/10 opacity-0 invisible group-hover:opacity-100 group-hover:visible transition-all duration-200 transform origin-top-right overflow-hidden">
                        <div class="py-2">
                            <repeat group="{{ @i18n.list.locales }}" value="{{ @localeName }}" key="{{ @localeCode }}">
                                <a href="/{{ substr(@localeCode, 0, 2) }}" class="language-switch flex items-center space-x-3 px-4 py-2 text-sm text-sukarix-ink/75 hover:bg-sukarix-lilac hover:text-sukarix-magenta" data-locale="{{ @localeCode }}">
                                    <span class="text-lg leading-none">{{ @locale_flags[@localeCode] }}</span>
                                    <span data-i18n-html="lst.locales.{{ @localeCode }}">{{ @localeName }}</span>
                                </a>
                            </repeat>
                        </div>
                    </div>
                </div>
            </div>

            <button id="mobile-menu-toggle" type="button" class="md:hidden inline-flex items-center justify-center p-2 rounded-md text-sukarix-ink/80 hover:text-sukarix-magenta hover:bg-sukarix-lilac focus:outline-none" aria-expanded="false" aria-controls="mobile-menu">
                <svg class="h-6 w-6" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                    <path stroke-linecap="round" stroke-linejoin="round" d="M4 6h16M4 12h16M4 18h16"/>
                </svg>
            </button>
        </div>
    </div>

    <div id="mobile-menu" class="mobile-menu md:hidden bg-white border-t border-sukarix-primary/10">
        <div class="px-4 pt-2 pb-4 space-y-1">
            <a href="{{ @ALIASES.home }}" class="block px-3 py-2 rounded-md text-base font-medium text-sukarix-ink/75 hover:text-sukarix-magenta hover:bg-sukarix-lilac" data-i18n-html="lbl.nav.home">{{ @i18n.label.nav.home }}</a>
            <a href="#features" class="block px-3 py-2 rounded-md text-base font-medium text-sukarix-ink/75 hover:text-sukarix-magenta hover:bg-sukarix-lilac" data-i18n-html="lbl.nav.features">{{ @i18n.label.nav.features }}</a>
            <a href="https://docs.sukarix.com" target="_blank" rel="noopener" class="block px-3 py-2 rounded-md text-base font-medium text-sukarix-ink/75 hover:text-sukarix-magenta hover:bg-sukarix-lilac" data-i18n-html="lbl.nav.documentation">{{ @i18n.label.nav.documentation }}</a>
            <p class="px-3 pt-3 text-xs font-semibold text-sukarix-primary/60 uppercase tracking-wider" data-i18n-html="lbl.nav.languages">{{ @i18n.label.nav.languages }}</p>
            <repeat group="{{ @i18n.list.locales }}" value="{{ @localeName }}" key="{{ @localeCode }}">
                <a href="/{{ substr(@localeCode, 0, 2) }}" class="language-switch flex items-center space-x-3 px-3 py-2 rounded-md text-base font-medium text-sukarix-ink/75 hover:text-sukarix-magenta hover:bg-sukarix-lilac" data-locale="{{ @localeCode }}">
                    <span class="text-lg leading-none">{{ @locale_flags[@localeCode] }}</span>
                    <span data-i18n-html="lst.locales.{{ @localeCode }}">{{ @localeName }}</span>
                </a>
            </repeat>
        </div>
    </div>
</nav>
